# deploy

Placeholder for **Phase 5** production deployment on the VPS: nginx site config (proxying to
`127.0.0.1:3100` and serving `data/photos` directly), a production Compose file, backups and TLS.
Nothing is built here yet.
